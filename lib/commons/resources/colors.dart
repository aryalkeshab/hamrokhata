import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 235, 96, 15);
const primaryColor2 = Color.fromRGBO(204, 31, 41, 1.0);

const primaryColorDark = Color.fromRGBO(227, 27, 40, 0.8);
const primaryColorLight = Color.fromRGBO(226, 35, 48, 1.0);
const secondaryColor = primaryColor;
const accentColor = Color(0xFFF2CA30);
const backgroundLight = Color(0xFFE6EBEB); //#E5E5E5
const scaffoldBackgroundColor = Color(0xFFF8F8F8);
const backgroundDark = Color(0xff141a31); //#E5E5E5
const secondaryColorDark = Color(0xFFFB8C00);
final textFieldBorderColor = Colors.grey[400];
final captionColor = Colors.grey[300];
final disabledColor = Colors.grey[400];

const dividerColor = Colors.grey;
const cardbodycolor = Colors.white70;

final grey400 = Colors.grey.withOpacity(0.4);
final grey500 = Colors.grey.withOpacity(0.5);
final darkGrey = Colors.black.withOpacity(0.6);
